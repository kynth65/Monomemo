
import { useLenis } from 'lenis/react';
import { PropsWithChildren } from 'react';

export function SmoothScroll({ children }: PropsWithChildren) {
    // Merge and apply options if needed
    // For example, you can pass options to useLenis
    // useLenis(options);

    return <>{children}</>;
}
